server {
    listen 80;
    # Serve the frontend HTML
    location / {
        proxy_pass http://localhost:8000/static/index.html;
    }
    # Forward API calls to FastAPI
    location /predict {
        proxy_pass http://localhost:8000/predict;
        proxy_set_header Content-Type application/json;
    }
}
